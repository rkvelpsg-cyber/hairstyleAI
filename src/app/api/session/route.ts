import { NextResponse } from "next/server";import fs from "node:fs/promises";import path from "node:path";import crypto from "node:crypto";
export async function POST(req:Request){try{const body=await req.json();const id=crypto.randomBytes(8).toString("hex");const dir=path.join(process.cwd(),".data","sessions");await fs.mkdir(dir,{recursive:true});await fs.writeFile(path.join(dir,`${id}.json`),JSON.stringify({id,createdAt:new Date().toISOString(),...body}));const base=process.env.NEXT_PUBLIC_PUBLIC_BASE_URL||"http://localhost:3000";return NextResponse.json({ok:true,url:`${base}/result/${id}`,id});}catch(e){return NextResponse.json({ok:false,error:"Unable to save session"},{status:500});}}
