import { NextResponse } from "next/server";
export async function POST(req:Request){try{const {amount,receipt}=await req.json();const id=process.env.RAZORPAY_KEY_ID,secret=process.env.RAZORPAY_KEY_SECRET;if(!id||!secret)return NextResponse.json({ok:false,error:"Razorpay is not configured"},{status:501});const auth=Buffer.from(`${id}:${secret}`).toString("base64");const r=await fetch("https://api.razorpay.com/v1/orders",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Basic ${auth}`},body:JSON.stringify({amount:Math.round(Number(amount)*100),currency:"INR",receipt})});const order=await r.json();if(!r.ok)return NextResponse.json({ok:false,error:order?.error?.description||"Order creation failed"},{status:502});return NextResponse.json({ok:true,order,keyId:id});}catch(e){return NextResponse.json({ok:false,error:"Payment error"},{status:500});}}
